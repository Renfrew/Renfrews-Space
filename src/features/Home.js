import React from 'react';

import Container from '@material-ui/core/Container';

const Home = (props) => {
  return (
    <Container component="main" disableGutters maxWidth={false}></Container>
  );
};

export default Home;
